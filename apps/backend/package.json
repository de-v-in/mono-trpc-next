{
  "name": "backend",
  "private": true,
  "scripts": {
    "dev": "bun --watch index.ts",
    "start": "bun run index.ts",
    "lint": "eslint .",
    "fix-lint": "eslint --fix ."
  },
  "dependencies": {
    "@repo/env": "*",
    "@repo/orm-entities": "*",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "@types/bun": "latest",
    "@types/cors": "^2.8.17"
  }
}
